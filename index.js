/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import PracticeNew from './src/PracticeNew';

AppRegistry.registerComponent(appName, () => PracticeNew);
