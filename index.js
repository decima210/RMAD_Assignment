/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import SampleJson from './src/SampleJason';

AppRegistry.registerComponent(appName, () => SampleJson);
