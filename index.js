/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import Assignment from './src/ConditionalStatement';

AppRegistry.registerComponent(appName, () => Assignment);
