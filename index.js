/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import PracticeConditions from './src/PracticeConditions';

AppRegistry.registerComponent(appName, () => PracticeConditions);
